export const deleteSelectedTopicsItem = (item, action) => {
    return {
        type: action,
        item
    }
}

export const addSelectedTopicsItem = (item, action) => {
    return {
        type: action,
        item
    }
}