export const deleteItem = (item, action) => {
    return {
        type: action,
        item
    }
}

export const addItem = (item, action) => {
    return {
        type: action,
        item
    }
}